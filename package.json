{
  "name": "minified-site",
  "version": "1.0.0",
  "scripts": {
    "build": "echo 'No build step necessary'",
    "start": "live-server public/"
  },
  "devDependencies": {
    "live-server": "^1.2.1"
  }
}
