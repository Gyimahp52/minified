{
  "name": "minified-site",
  "version": "1.0.0",
  "description": "A site with minified assets",
  "scripts": {
    "build": "echo 'No build step necessary'",
    "start": "live-server public/"
  },
  "devDependencies": {
    "live-server": "^1.2.1"
  }
}
