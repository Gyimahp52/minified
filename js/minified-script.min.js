// Simulate loading performance metrics for minified page
document.addEventListener("DOMContentLoaded",function(){const data={loadTime:600,pageSize:5000};document
.getElementById("load-time").textContent=data.loadTime,document
.getElementById("page-size").textContent=data.pageSize});
